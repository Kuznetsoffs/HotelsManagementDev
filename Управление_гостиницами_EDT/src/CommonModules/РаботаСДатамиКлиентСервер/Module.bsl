Функция ПрибавитьДень(Дата,КоличествоДней) Экспорт
	
	Приращение = КоличествоДней * 60 * 60 * 24;
	Возврат Дата + Приращение;

КонецФункции // ПрибавитьДень()

Функция КоличествоДней(НачДата,КонДата) Экспорт
	Если НачДата>КонДата Тогда
		Возврат Цел((НачалоДня(НачДата) - НачалоДня(КонДата)) / 86400);
	Иначе
		
		Возврат Цел((НачалоДня(КонДата) - НачалоДня(НачДата)) / 86400);
	КонецЕсли; 
	
	
КонецФункции // КоличествоДней()
