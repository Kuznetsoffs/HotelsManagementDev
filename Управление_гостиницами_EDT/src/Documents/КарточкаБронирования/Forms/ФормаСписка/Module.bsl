&НаСервере
Функция ИспользоватьИнтерактивныйВвод()

	Возврат Константы.ИнтерактивныйВводКарточкиБрони.Получить();

КонецФункции // ИспользоватьИнтерактивныйВвод()


&НаКлиенте
Процедура СписокПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	Если НЕ Копирование и ИспользоватьИнтерактивныйВвод() Тогда
		Отказ = Истина;
	КонецЕсли; 
КонецПроцедуры
